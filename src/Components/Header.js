import React, {Component} from 'react'
import {View, Text} from 'react-native'

export default function Header(){
  return(
    <View style={{flex:1}}>
        <Text style={{fontSize:24}}>Danielli Sant' Anna</Text>
    </View>
  )
}